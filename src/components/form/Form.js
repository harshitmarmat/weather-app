import classes from './Form.module.css';

const Form = () => {
    return(
        <form>
            <label>Enter the city name : </label>
            <input/>
        </form>
    )
}

export default Form;
